# RecipeApp

A standalone Flask app for saving public recipe URLs and normalizing ingredient units to metric.

## Run

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Open `http://127.0.0.1:5000`.
